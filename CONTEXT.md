# PlogKit

Plog 创作者在发布前整理照片、文字与构图的领域语言。

## 编辑

**统一文档（Document）**：
描述作品内容与构图的可保存状态，是编辑、渲染、撤销、保存与导出的共同对象。
_避免使用_：画布状态、编辑器状态

**编辑提交（Edit Commit）**：
一次有意义的用户交互，使统一文档发生一次可撤销、可自动保存的变化。实时预览不属于编辑提交。
_避免使用_：编辑事务、变更提交

**编辑意图（Edit Intent）**：
用户对统一文档提出的一次语义变化请求，可用于实时预览或形成编辑提交；它本身不是可保存的作品状态。
_避免使用_：编辑命令、操作对象

**文本块（Text Block）**：
统一文档中一段可编辑、可摆放的文字内容。
_避免使用_：Paragraph 对象、文字命中框

## 草稿与会话

**草稿（Draft）**：
用户可长期保留、重新打开并继续编辑的作品状态，至少包含一个导入资产。每个草稿独立拥有自己的统一文档与本地素材。
_避免使用_：项目、历史编辑会话

**草稿库（Draft Library）**：
设备本地保存的草稿集合，用户可从中创建、选择和管理草稿。
_避免使用_：项目列表、历史会话列表

**草稿缩略图（Draft Thumbnail）**：
草稿当前完整构图的可重建视觉摘要。
_避免使用_：草稿封面、第一张照片预览

**草稿元数据（Draft Metadata）**：
随草稿保存、用于描述和排列草稿的属性，如创建时间与最近编辑时间。
_避免使用_：照片拍摄信息、导出元数据

**内容修订（Content Revision）**：
草稿成功持久化的一代不同统一文档；多个编辑提交可以合并为同一内容修订。
_避免使用_：编辑次数、版本历史、保存次数

**损坏草稿（Corrupt Draft）**：
已经成功创建，但保存的内容不完整或彼此不一致，因而不能安全打开的草稿。缩略图或编辑预览缺失不构成草稿损坏。
_避免使用_：预览缺失、临时存储故障

**当前编辑会话（Current Editing Session）**：
用户当前打开一个草稿进行编辑的临时活动。
_避免使用_：编辑器运行时、当前项目、草稿

## 素材

**导入资产（Imported Asset）**：
已经复制到草稿并由该草稿拥有的单张图片素材。
_避免使用_：共享素材、图片缓存、相册引用

## 导出

**导出预设（Export Preset）**：
App 提供的一套导出规则，用于确定格式、尺寸与照片信息策略。
_避免使用_：导出配置快照、平台压缩保证
