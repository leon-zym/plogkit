# Package registry mirror for reliable downloads
registry=https://registry.npmmirror.com/

# Increase network fetch timeout and retry attempts for large binary dependencies
fetch-timeout=600000
fetch-retries=5

# Limit network concurrency to prevent timeouts on unstable connections
network-concurrency=2
